-- Generated by Prisma migrate diff. Source: prisma/base.prisma (mysql).
-- Do not edit this file directly; update the Prisma schema and rerun npm run prisma:migrations.

-- CreateTable
CREATE TABLE `base_tenants` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `name` VARCHAR(64) NOT NULL,
    `title` VARCHAR(191) NOT NULL,
    `status` ENUM('enabled', 'disabled') NOT NULL DEFAULT 'enabled',

    UNIQUE INDEX `base_tenants_key_key`(`key`),
    UNIQUE INDEX `base_tenants_name_deleted_at_key`(`name`, `deleted_at`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_branches` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `name` VARCHAR(64) NOT NULL,
    `title` VARCHAR(191) NOT NULL,
    `status` ENUM('enabled', 'disabled') NOT NULL DEFAULT 'enabled',

    UNIQUE INDEX `base_branches_key_key`(`key`),
    UNIQUE INDEX `base_branches_owner_tid_name_deleted_at_key`(`owner_tid`, `name`, `deleted_at`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_hosts` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `hostname` VARCHAR(191) NOT NULL,
    `tenant_id` BIGINT NOT NULL,
    `branch_id` BIGINT NOT NULL,
    `status` ENUM('enabled', 'disabled') NOT NULL DEFAULT 'enabled',

    INDEX `base_hosts_tenant_id_idx`(`tenant_id`),
    INDEX `base_hosts_branch_id_idx`(`branch_id`),
    UNIQUE INDEX `base_hosts_hostname_key`(`hostname`),
    UNIQUE INDEX `base_hosts_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_users` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `agent_uid` BIGINT NULL,
    `name` VARCHAR(191) NOT NULL,
    `roles` VARCHAR(191) NOT NULL DEFAULT '[]',
    `status` ENUM('enabled', 'disabled') NOT NULL DEFAULT 'enabled',

    INDEX `base_users_agent_uid_idx`(`agent_uid`),
    UNIQUE INDEX `base_users_owner_tid_name_deleted_at_key`(`owner_tid`, `name`, `deleted_at`),
    UNIQUE INDEX `base_users_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_sessions` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `token_hash` VARCHAR(191) NOT NULL,
    `user_id` BIGINT NOT NULL,
    `expires_at` BIGINT NOT NULL,
    `device_id` BIGINT NOT NULL,

    INDEX `base_sessions_user_id_idx`(`user_id`),
    INDEX `base_sessions_device_id_idx`(`device_id`),
    UNIQUE INDEX `base_sessions_token_hash_key`(`token_hash`),
    UNIQUE INDEX `base_sessions_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_configs` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `name` VARCHAR(64) NOT NULL,
    `value` VARCHAR(191) NOT NULL,

    UNIQUE INDEX `base_configs_key_key`(`key`),
    UNIQUE INDEX `base_configs_owner_tid_name_deleted_at_key`(`owner_tid`, `name`, `deleted_at`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_bootstraps` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `name` VARCHAR(64) NOT NULL,
    `value` VARCHAR(191) NOT NULL,

    UNIQUE INDEX `base_bootstraps_key_key`(`key`),
    UNIQUE INDEX `base_bootstraps_owner_tid_name_deleted_at_key`(`owner_tid`, `name`, `deleted_at`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_oidc_login_requests` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `request_id` VARCHAR(191) NOT NULL,
    `issuer` VARCHAR(191) NOT NULL,
    `state` VARCHAR(191) NOT NULL,
    `nonce` VARCHAR(191) NOT NULL,
    `code_verifier` VARCHAR(191) NOT NULL,
    `return_path` VARCHAR(191) NOT NULL DEFAULT '/',
    `expires_at` BIGINT NOT NULL,
    `status` VARCHAR(191) NOT NULL DEFAULT 'pending',
    `subject` VARCHAR(191) NOT NULL DEFAULT '',
    `claims` VARCHAR(191) NOT NULL DEFAULT '{}',
    `credential` VARCHAR(191) NOT NULL DEFAULT '',

    UNIQUE INDEX `base_oidc_login_requests_request_id_key`(`request_id`),
    UNIQUE INDEX `base_oidc_login_requests_state_key`(`state`),
    UNIQUE INDEX `base_oidc_login_requests_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_oidc_users` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `issuer` VARCHAR(191) NOT NULL,
    `subject` VARCHAR(191) NOT NULL,
    `user_id` BIGINT NOT NULL,
    `profile` VARCHAR(191) NOT NULL DEFAULT '{}',

    INDEX `base_oidc_users_user_id_idx`(`user_id`),
    UNIQUE INDEX `base_oidc_users_owner_tid_issuer_subject_key`(`owner_tid`, `issuer`, `subject`),
    UNIQUE INDEX `base_oidc_users_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_oidc_sessions` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `issuer` VARCHAR(191) NOT NULL,
    `sid` VARCHAR(191) NOT NULL,
    `session_id` BIGINT NOT NULL,

    UNIQUE INDEX `base_oidc_sessions_session_id_key`(`session_id`),
    UNIQUE INDEX `base_oidc_sessions_issuer_sid_key`(`issuer`, `sid`),
    UNIQUE INDEX `base_oidc_sessions_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_devices` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `user_id` BIGINT NOT NULL,
    `fingerprint` JSON NOT NULL,
    `user_agent` VARCHAR(191) NOT NULL DEFAULT '',
    `platform` VARCHAR(191) NOT NULL DEFAULT '',
    `ip_address` VARCHAR(191) NOT NULL DEFAULT '',
    `network_info` JSON NOT NULL,
    `status` ENUM('active', 'revoked') NOT NULL DEFAULT 'active',
    `last_seen_at` BIGINT NOT NULL,
    `revoked_at` BIGINT NULL,

    INDEX `base_devices_user_id_idx`(`user_id`),
    UNIQUE INDEX `base_devices_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_device_users` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `device_id` BIGINT NOT NULL,
    `user_id` BIGINT NOT NULL,
    `status` ENUM('active', 'revoked') NOT NULL DEFAULT 'active',
    `last_seen_at` BIGINT NOT NULL,
    `revoked_at` BIGINT NULL,

    UNIQUE INDEX `base_device_users_device_id_user_id_key`(`device_id`, `user_id`),
    UNIQUE INDEX `base_device_users_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_device_snapshots` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `device_id` BIGINT NOT NULL,
    `fingerprint` JSON NOT NULL,
    `ip_address` VARCHAR(191) NOT NULL DEFAULT '',
    `network_info` JSON NOT NULL,
    `user_agent` VARCHAR(191) NOT NULL DEFAULT '',
    `platform` VARCHAR(191) NOT NULL DEFAULT '',
    `screen_width` INTEGER NOT NULL DEFAULT 0,
    `screen_height` INTEGER NOT NULL DEFAULT 0,
    `captured_at` BIGINT NOT NULL,

    INDEX `base_device_snapshots_device_id_idx`(`device_id`),
    UNIQUE INDEX `base_device_snapshots_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_audit_transitions` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `audit_id` BIGINT NOT NULL,
    `kind` ENUM('approve', 'reject', 'withdraw', 'requeue', 'revert', 'redo') NOT NULL,
    `reason` VARCHAR(191) NOT NULL DEFAULT '',

    INDEX `base_audit_transitions_audit_id_id_idx`(`audit_id`, `id`),
    UNIQUE INDEX `base_audit_transitions_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_audits` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `operation_id` VARCHAR(191) NOT NULL,
    `reason` VARCHAR(191) NOT NULL DEFAULT '',
    `scope` ENUM('admin', 'self') NOT NULL DEFAULT 'admin',
    `request_hostname` VARCHAR(191) NOT NULL DEFAULT '',
    `request_path` VARCHAR(191) NOT NULL DEFAULT '',
    `table_name` VARCHAR(191) NOT NULL,
    `row_id` BIGINT NOT NULL,
    `row_key` VARCHAR(36) NOT NULL DEFAULT '',
    `action` ENUM('insert', 'update', 'soft_delete', 'restore', 'purge') NOT NULL,
    `changes_before` JSON NOT NULL,
    `changes_after` JSON NOT NULL,
    `review_status` ENUM('none', 'pending', 'approved', 'rejected', 'withdrawn') NOT NULL DEFAULT 'none',
    `data_status` ENUM('unwritten', 'applied', 'reverted') NOT NULL DEFAULT 'applied',
    `settled_at` BIGINT NOT NULL DEFAULT 0,

    INDEX `base_audits_owner_tid_created_at_idx`(`owner_tid`, `created_at`),
    INDEX `base_audits_owner_bid_created_at_idx`(`owner_bid`, `created_at`),
    INDEX `base_audits_owner_uid_created_at_idx`(`owner_uid`, `created_at`),
    INDEX `base_audits_table_name_row_id_created_at_idx`(`table_name`, `row_id`, `created_at`),
    INDEX `base_audits_operation_id_idx`(`operation_id`),
    INDEX `base_audits_review_status_created_at_idx`(`review_status`, `created_at`),
    INDEX `base_audits_scope_created_at_idx`(`scope`, `created_at`),
    UNIQUE INDEX `base_audits_table_name_row_key_settled_at_key`(`table_name`, `row_key`, `settled_at`),
    UNIQUE INDEX `base_audits_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_user_credentials` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `user_id` BIGINT NOT NULL,
    `password` JSON NOT NULL,

    UNIQUE INDEX `base_user_credentials_user_id_key`(`user_id`),
    UNIQUE INDEX `base_user_credentials_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `base_user_profiles` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `key` VARCHAR(36) NOT NULL,
    `created_at` BIGINT NOT NULL,
    `updated_at` BIGINT NOT NULL,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `queued_at` BIGINT NOT NULL DEFAULT 0,
    `created_duid` BIGINT NULL,
    `updated_duid` BIGINT NULL,
    `owner_tid` BIGINT NOT NULL DEFAULT 1,
    `owner_bid` BIGINT NOT NULL DEFAULT 1,
    `owner_uid` BIGINT NULL,
    `user_id` BIGINT NOT NULL,
    `nickname` VARCHAR(191) NULL,
    `qq` VARCHAR(191) NULL,
    `wechat` VARCHAR(191) NULL,
    `email` VARCHAR(191) NULL,

    UNIQUE INDEX `base_user_profiles_user_id_key`(`user_id`),
    UNIQUE INDEX `base_user_profiles_owner_tid_nickname_key`(`owner_tid`, `nickname`),
    UNIQUE INDEX `base_user_profiles_key_key`(`key`),
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
